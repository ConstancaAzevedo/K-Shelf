@page
@model K_Shelf.Pages.Grupos.DeleteModel
@{
    ViewData["Title"] = "Eliminar Grupo";
}

<div class="container py-4 animate-fade-in-up">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <!-- painel principal com efeito glassmorphism -->
            <div class="glass-panel p-4 shadow-sm">
                <!-- cabecalho com icone de alerta e titulo -->
                <div class="d-flex align-items-center mb-4">
                    <span class="fs-1 me-3">🗑️</span> <!-- icone de eliminar -->
                    <div>
                        <h2 class="fw-bold mb-0 text-danger">Eliminar Grupo</h2> <!-- titulo em vermelho -->
                        <p class="text-secondary mb-0">Esta ação é irreversível</p> <!-- aviso de irreversibilidade -->
                    </div>
                </div>

                <!-- alerta de confirmacao de eliminacao -->
                <div class="alert alert-warning rounded-3 mb-4">
                    <h5 class="fw-bold mb-1">Tem a certeza que pretende eliminar este grupo?</h5>
                    <p class="mb-0">Todos os artistas e álbuns associados serão afetados.</p> <!-- aviso sobre os artistas e albuns associados -->
                </div>

                <!-- informacoes do grupo a eliminar -->
                <div class="p-3 bg-light rounded-3 mb-4">
                    <h4 class="fw-bold mb-1">@Model.Grupo.Nome</h4> <!-- nome do grupo -->
                    <p class="text-secondary mb-1">
                        <strong>Data Estreia:</strong> @Model.Grupo.DataEstreia.ToString("dd/MM/yyyy") <!-- data de estreia formatada -->
                    </p>
                    <p class="text-secondary mb-1">
                        <strong>Companhia:</strong> @(Model.Grupo.Companhia ?? "N/A") <!-- companhia ou N/A se for nula -->
                    </p>
                    <p class="text-secondary mb-1">
                        <strong>Fansigno:</strong> @(Model.Grupo.Fansigno ?? "N/A") <!-- fansigno ou N/A se for nulo -->
                    </p>
                    <p class="text-secondary mb-1">
                        <strong>Status:</strong> @(Model.Grupo.IsAtivo ? "Ativo" : "Inativo") <!-- indica se o grupo esta ativo ou inativo -->
                    </p>
                </div>

                <!-- formulario de confirmacao de eliminacao -->
                <form method="post">
                    <!-- campo escondido para enviar o id do grupo -->
                    <input type="hidden" asp-for="Grupo.Id" />
                    <!-- botoes de acao -->
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-danger rounded-pill px-4">Confirmar Eliminação</button> <!-- botao de confirmacao -->
                        <a asp-page="./Index" class="btn btn-outline-secondary rounded-pill px-4">Cancelar</a> <!-- volta para a lista -->
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>